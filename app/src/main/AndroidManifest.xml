<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.visight.adondevamos">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning"
            android:usesCleartextTraffic="true">

        <activity android:name=".ui.start.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.start.StartActivity" />
        <activity android:name=".ui.start.login.LoginActivity"/>
        <activity android:name=".ui.start.register.RegisterActivity"/>
        <activity android:name=".ui.main.user.MainActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".ui.main.user.ProfileActivity"/>
        <activity android:name=".ui.main.place.PlaceDetailActivity"/>
        <activity android:name=".ui.main.place.report.ReportFromPlaceActivity"/>
        <activity android:name=".ui.main.user.favourites.FavouritesActivity"/>
        <activity android:name=".ui.main.user.info.InformationActivity"/>
        <activity android:name=".ui.main.user.info.TermsPolicyActivity"/>

        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="@string/maps_release_api_key"/>

        <!-- File provider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>

</manifest>